name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  # ---------------------------------------------------------------
  # Job 1: Build the main CC Director Avalonia app (Windows)
  # ---------------------------------------------------------------
  # CcDirector.Wpf was archived in commit c557e58c -- the Avalonia app
  # is now the canonical main app for both Windows and macOS.
  build-director-win:
    name: Build CC Director (Windows)
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Publish for Windows x64
        run: >
          dotnet publish src/CcDirector.Avalonia/CcDirector.Avalonia.csproj
          -c Release -r win-x64 --self-contained true
          -p:PublishSingleFile=true
          -p:IncludeNativeLibrariesForSelfExtract=true

      - name: Upload cc-director artifact
        uses: actions/upload-artifact@v4
        with:
          name: cc-director-win
          path: src/CcDirector.Avalonia/bin/Release/net10.0/win-x64/publish/cc-director.exe
          if-no-files-found: error

  # ---------------------------------------------------------------
  # Job 2: Build CC Director Avalonia app (macOS)
  # ---------------------------------------------------------------
  build-director-mac:
    name: Build CC Director (macOS)
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Publish for macOS ARM64
        run: >
          dotnet publish src/CcDirector.Avalonia/CcDirector.Avalonia.csproj
          -c Release -r osx-arm64 --self-contained true
          -p:PublishSingleFile=true

      - name: Upload cc-director-mac artifact
        uses: actions/upload-artifact@v4
        with:
          name: cc-director-mac
          path: src/CcDirector.Avalonia/bin/Release/net10.0/osx-arm64/publish/cc-director
          if-no-files-found: error

  # ---------------------------------------------------------------
  # Job 3: Build Python tools (Windows)
  # ---------------------------------------------------------------
  build-python-tools-win:
    name: Build Python Tools (Windows)
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Create artifacts directory
        run: New-Item -ItemType Directory -Path artifacts -Force

      - name: Build Python tools
        shell: pwsh
        run: |
          $tools = @(
            "cc-pdf",
            "cc-html",
            "cc-word"
          )

          $failed = @()

          foreach ($tool in $tools) {
            $toolDir = "tools/$tool"
            $buildScript = "$toolDir/build.ps1"

            if (-not (Test-Path $buildScript)) {
              Write-Host "[SKIP] $tool - no build.ps1" -ForegroundColor Yellow
              continue
            }

            Write-Host ""
            Write-Host "============================================" -ForegroundColor Cyan
            Write-Host "Building $tool..." -ForegroundColor Cyan
            Write-Host "============================================" -ForegroundColor Cyan

            Push-Location $toolDir
            try {
              & powershell -ExecutionPolicy Bypass -File build.ps1
              if ($LASTEXITCODE -ne 0) {
                Write-Host "[FAIL] $tool build failed" -ForegroundColor Red
                $failed += $tool
                continue
              }

              $exeName = "$tool.exe"
              $exePath = "dist/$exeName"
              if (Test-Path $exePath) {
                Copy-Item $exePath "../../artifacts/$exeName" -Force
                Write-Host "[OK] $exeName" -ForegroundColor Green
              } else {
                Write-Host "[FAIL] $exeName not found after build" -ForegroundColor Red
                $failed += $tool
              }
            } finally {
              Pop-Location
            }
          }

          $built = $tools.Count - $failed.Count
          Write-Host ""
          Write-Host "Python tools built: $built / $($tools.Count)" -ForegroundColor Cyan
          if ($failed.Count -gt 0) {
            Write-Host "Failed tools: $($failed -join ', ')" -ForegroundColor Red
            exit 1
          }

      - name: Upload Python tools artifact
        uses: actions/upload-artifact@v4
        with:
          name: python-tools-win
          path: artifacts/
          if-no-files-found: error

  # ---------------------------------------------------------------
  # Job 4: Build Python tools (macOS)
  # ---------------------------------------------------------------
  build-python-tools-mac:
    name: Build Python Tools (macOS)
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Build Python tools
        run: |
          tools=("cc-pdf" "cc-html" "cc-word")
          mkdir -p artifacts
          failed=()

          for tool in "${tools[@]}"; do
            echo ""
            echo "============================================"
            echo "Building $tool..."
            echo "============================================"

            cd "tools/$tool"

            # Install dependencies
            pip install -e ".[dev]" 2>&1
            pip install -e "../cc_shared" 2>&1

            # Build with PyInstaller using the .spec file if available
            if [ -f "$tool.spec" ]; then
              pyinstaller "$tool.spec" --clean --noconfirm
            else
              pyinstaller --onefile --name "$tool" main.py
            fi

            if [ -f "dist/$tool" ]; then
              cp "dist/$tool" "../../artifacts/$tool"
              echo "[OK] $tool"
            else
              echo "[FAIL] $tool not found after build"
              failed+=("$tool")
            fi

            cd ../..
          done

          echo ""
          echo "Python tools built: $(( ${#tools[@]} - ${#failed[@]} )) / ${#tools[@]}"
          if [ ${#failed[@]} -gt 0 ]; then
            echo "Failed tools: ${failed[*]}"
            exit 1
          fi

      - name: Upload Python tools artifact
        uses: actions/upload-artifact@v4
        with:
          name: python-tools-mac
          path: artifacts/
          if-no-files-found: error

  # ---------------------------------------------------------------
  # Job 5: Build Setup Wizard (Windows)
  # ---------------------------------------------------------------
  build-setup-wizard-win:
    name: Build Setup Wizard (Windows)
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Publish self-contained setup wizard
        run: >
          dotnet publish tools/cc-director-setup/CcDirectorSetup.csproj
          -c Release -r win-x64 --self-contained true
          -p:PublishSingleFile=true
          -p:IncludeNativeLibrariesForSelfExtract=true
          --nologo

      - name: Upload setup wizard artifact
        uses: actions/upload-artifact@v4
        with:
          name: setup-wizard-win
          path: tools/cc-director-setup/bin/Release/net10.0-windows/win-x64/publish/cc-director-setup.exe
          if-no-files-found: error

  # ---------------------------------------------------------------
  # Job 6: Build Setup Wizard (macOS)
  # ---------------------------------------------------------------
  build-setup-wizard-mac:
    name: Build Setup Wizard (macOS)
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Publish for macOS ARM64
        run: >
          dotnet publish tools/cc-director-setup-avalonia/CcDirectorSetup.csproj
          -c Release -r osx-arm64 --self-contained true
          -p:PublishSingleFile=true

      - name: Upload setup wizard artifact
        uses: actions/upload-artifact@v4
        with:
          name: setup-wizard-mac
          path: tools/cc-director-setup-avalonia/bin/Release/net10.0/osx-arm64/publish/cc-director-setup
          if-no-files-found: error

  # ---------------------------------------------------------------
  # Final Job: Create GitHub Release
  # ---------------------------------------------------------------
  create-release:
    name: Create Release
    runs-on: ubuntu-latest
    needs:
      - build-director-win
      - build-director-mac
      - build-python-tools-win
      - build-python-tools-mac
      - build-setup-wizard-win
      - build-setup-wizard-mac

    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-artifacts

      - name: List artifacts
        run: find release-artifacts -type f | head -50

      - name: Collect release files
        run: |
          RELEASE_DIR="$(pwd)/release-files"
          mkdir -p "$RELEASE_DIR"

          # Windows main app
          cp release-artifacts/cc-director-win/cc-director.exe "$RELEASE_DIR/cc-director-win-x64.exe"

          # macOS main app
          cp release-artifacts/cc-director-mac/cc-director "$RELEASE_DIR/cc-director-mac-arm64"

          # Windows Python tools
          for exe in release-artifacts/python-tools-win/*.exe; do
            name=$(basename "$exe" .exe)
            cp "$exe" "$RELEASE_DIR/${name}-win-x64.exe"
          done

          # macOS Python tools
          for bin in release-artifacts/python-tools-mac/*; do
            name=$(basename "$bin")
            cp "$bin" "$RELEASE_DIR/${name}-mac-arm64"
          done

          # Windows setup wizard
          cp release-artifacts/setup-wizard-win/cc-director-setup.exe "$RELEASE_DIR/cc-director-setup-win-x64.exe"

          # macOS setup wizard
          cp release-artifacts/setup-wizard-mac/cc-director-setup "$RELEASE_DIR/cc-director-setup-mac-arm64"

          echo ""
          echo "Release files:"
          ls -lh "$RELEASE_DIR/"

      - name: Generate release manifest
        run: |
          RELEASE_DIR="$(pwd)/release-files"
          VERSION="${GITHUB_REF_NAME#v}"
          MANIFEST="$RELEASE_DIR/release-manifest.json"

          echo "{" > "$MANIFEST"
          echo "  \"version\": \"$VERSION\"," >> "$MANIFEST"
          echo "  \"tag\": \"$GITHUB_REF_NAME\"," >> "$MANIFEST"
          echo "  \"date\": \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"," >> "$MANIFEST"
          echo "  \"assets\": {" >> "$MANIFEST"

          first=true
          for file in "$RELEASE_DIR"/*; do
            filename=$(basename "$file")
            # Skip the manifest itself
            if [ "$filename" = "release-manifest.json" ]; then
              continue
            fi
            size=$(stat -c%s "$file")
            sha256=$(sha256sum "$file" | cut -d' ' -f1)

            # Determine platform from filename
            if echo "$filename" | grep -q "win-x64"; then
              platform="windows"
            elif echo "$filename" | grep -q "mac-arm64"; then
              platform="macos"
            else
              platform="unknown"
            fi

            if [ "$first" = true ]; then
              first=false
            else
              echo "," >> "$MANIFEST"
            fi

            printf "    \"%s\": {\"size\": %s, \"sha256\": \"%s\", \"platform\": \"%s\"}" "$filename" "$size" "$sha256" "$platform" >> "$MANIFEST"
          done

          echo "" >> "$MANIFEST"
          echo "  }" >> "$MANIFEST"
          echo "}" >> "$MANIFEST"

          echo "Release manifest:"
          cat "$MANIFEST"

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: CC Director v${{ steps.version.outputs.version }}
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
          generate_release_notes: true
          files: release-files/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
