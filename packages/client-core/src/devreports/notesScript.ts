// The note-taking script, bundled from its ONE source file (mission ruling 5). Every host injects this
// string; there is no copy of the script anywhere else.
import source from "./dev-report-notes.js?raw";

export const DEV_REPORT_NOTES_SCRIPT: string = source;
