# Capture: permission-prompt

Session b7151d5a-ccdc-4e9f-8daa-78a50b654861, Director :7887, state at capture: WaitingForInput

## What the USER SEES (screen grid, last 25 rows)

```
???? Claude Code v2.1.163 ??????????????????????????????????????????????????????????????????????????????????????????????
?                                                    ? Tips for getting started                                        ?
?                 Welcome back!                     ? Run /init to create a CLAUDE.md file with instructions for Cla? ?
?                                                    ? ??????????????????????????????????????????????????????????????? ?
?                       ???????                      ? What's new                                                      ?
?                      ?????????                     ? Added `requiredMinimumVersion` and `requiredMaximumVersion` ma? ?
?                        ?? ??                       ? Added `/plugin list` command to list installed plugins, with `? ?
?    Opus 4.8 (1M context) ? Claude Max ?            ? Added a "c to copy" shortcut to `/btw` that copies the raw mar? ?
?    user@example.com's Organization    ? /release-notes for more                                         ?
?        D:\ReposFred\devthrottle-qa-scratch         ?                                                                 ?
????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????
? Run this bash command: git -C . log --oneline -3                                                                      ??Try "edit <filepath> to..."
? Bash(git -C . log --oneline -3)                                                                                         ? ?Waiting?
 Bash command
????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????
 Bash command
   git -C . log --oneline -3
   Show last 3 commits
 This command requires approval
 Do you want to proceed?
 ? 1. Yes
   2. Yes, and don?t ask again for: git *
   3. No
 Esc to cancel ? Tab to amend ? ctrl+e to explain
```

## What the TRANSCRIPT knows (parsed widgets)

```
  0. UserMessage: Run this bash command: git -C . log --oneline -3
```

## What Brief v1 made of it (GET /brief)

```json
{
  "status": "ok",
  "activityState": "WaitingForInput",
  "replyPending": true,
  "goal": "Run this bash command: git -C . log --oneline -3",
  "lastAsk": "Run this bash command: git -C . log --oneline -3",
  "didBullets": [],
  "needsYou": null,
  "needsYouSource": null,
  "condenser": "unavailable"
}
```

## Correct TurnBrief (authored by the strong model - the quality bar)

```json
{
  "intent": "You asked Claude to run a git command in a permissions-on session.",
  "did": [ "Requested approval to run: git -C . log --oneline -3 (show last 3 commits)" ],
  "needsYou": {
    "statement": "Claude wants permission to run a READ-ONLY git command (git log,
                  last 3 commits). Allow once, allow ALL git commands from now on
                  (a standing grant - broader than this request), or deny.",
    "answerVia": "keys",
    "options": [
      { "key": "1 Allow once",            "send": "1" },
      { "key": "2 Always allow git *",    "send": "2", "note": "standing grant" },
      { "key": "3 Deny",                  "send": "3" }
    ],
    "evidence": "Bash command / git -C . log --oneline -3 / Show last 3 commits /
                 This command requires approval / Do you want to proceed? / 1. Yes /
                 2. Yes, and don't ask again for: git * / 3. No",
    "urgency": "blocking",
    "confidence": "high",
    "railLine": "Approve git log? (option 2 = standing git grant)"
  }
}
```

## Findings

- Transcript blind again (one widget); the screen has the full dialog including the
  command, its plain-English description, and the option semantics.
- THE BRIEF MUST SURFACE SCOPE: option 2 is not "yes" - it grants standing permission
  for ALL git commands. A brief that flattens the three options into approve/deny is
  dangerous. The wingman's risk-awareness here (read-only command, broad grant on
  option 2) is exactly the interpretation a rule could never do.
- Answerable by keystrokes; same `answerVia: "keys"` mechanics as the picker.
