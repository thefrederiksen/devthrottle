<Project Sdk="Microsoft.NET.Sdk">

  <!--
    THROWAWAY SPIKE PROOF-OF-CONCEPT for issue 627.
    This project is deliberately NOT part of cc-director.sln. It exists only to
    produce REAL observed ACP wire transcripts from a live copilot acp server.
    It ships NO production code and must never be referenced by production projects.
  -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AssemblyName>AcpPoc</AssemblyName>
    <RootNamespace>AcpPoc</RootNamespace>
  </PropertyGroup>

</Project>
