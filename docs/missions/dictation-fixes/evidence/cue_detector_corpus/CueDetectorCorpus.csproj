<Project Sdk="Microsoft.NET.Sdk">

  <!-- Mission evidence harness (phase 6, ruling K1), not product code and not in the solution. Runs the
       product's ReadyCue.Find over local corpus clips; audio and transcripts never leave the machine. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\..\..\..\src\CcDirector.Core\CcDirector.Core.csproj" />
  </ItemGroup>

</Project>
