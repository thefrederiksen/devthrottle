# Inspection 8: slice 2 fix round 2

## Verdict: FAIL for merge

The round closes the ordinary trailing-whitespace, stuck-notice, and long-dictation cases. One exact-match cleanup path still permits deletion of owner input. This review is of `b7bdb3cf...HEAD` under `src`, `tools`, and `packages`, with a whole-branch check against `origin/main...HEAD`.

## Findings, ranked

1. **High — a trimmed owner character with no cursor advance is treated as the doorbell's own line.** `DoorbellSafety.ComposerHoldsExactly` matches the rendered segments and uses cursor visibility, row, and column as its only witness for trimmed trailing characters (`src/CcDirector.Core/Drivers/DoorbellSafety.cs:349-382`). If the owner appends a non-breaking space that the grid trims and the terminal does not advance the cursor for it, the frame is identical to the unextended line, so the method returns true. `FleetDoorbellRinger.TakeBackUnverifiedAsync` then sends `line.Length` Backspaces (`src/CcDirector.Core/Sessions/FleetDoorbellRinger.cs:231-246`). This violates the Architect's “any other difference, leave it” cleanup ruling (`docs/missions/message-load-2026-09-16/handoff.md:1046-1049`). Prove with a scripted unverified-submit frame whose row is the bare doorbell line, cursor is visible at the bare line's final column, but the backing composer contains that line followed by a non-breaking space; record the erase call. A hidden cursor is safely rejected at `DoorbellSafety.cs:380`. This is an indistinguishable-screen limit: the current frame cannot prove the character absent. The round's extended-line guards all move the cursor one further or leave the character visible (`src/CcDirector.Core.UnitTests/Sessions/FleetDoorbellRingerTests.cs:262-308`). The builder explicitly records a similar unobservable wrap-break case at `DoorbellSafety.cs:346-347`; neither meets the literal exactness ruling.

2. **Low — fitting below the notice's fixed prefix can remove the message id.** `StuckNoticeText` truncates the bare notice from the right when the cap is shorter than the bare text (`src/CcDirector.Gateway/Messaging/FleetDoorbell.cs:481-495`). A cap shorter than the prefix plus the 32-character id yields a notice without the full id; at still smaller caps, two messages can share the same text. Prove by calling it for two ids with `maxLength: 20` and comparing both strings and id inclusion. The current product cap is 16,000 (`src/CcDirector.Gateway/Messaging/FleetMessageLimits.cs:46-48`), so this is not a reachable product refusal or a separate merge blocker today. It narrows the builder's unconditional claim that a fitted notice always names its message. The present test uses a 60-character cap, which retains the id (`src/CcDirector.Gateway.UnitTests/Messaging/FleetDoorbellTests.cs:459-472`).

## Inspection-5 closure and challenged paths

1. **Exact cleanup: reopened by finding 1.** Visible owner text before the line fails the ordinal segment match (`DoorbellSafety.cs:376-401`); a hidden cursor fails the cursor check. The second terminal reader uses the cursor row and the same exact match (`DoorbellSafety.cs:368-382`). A line extended while it echoes can still be submitted: the submit path accepts either byte echo or the screen witness, waits 40 ms, then presses Enter without a new composer check (`src/CcDirector.Core/Drivers/TerminalSubmit.cs:238-253`). The last look guards only before the first byte (`src/CcDirector.Core/Sessions/FleetDoorbellRinger.cs:155-163`). This is the disclosed race, not a new regression from this round.
2. **Stuck notice: closed for the product cap.** The store stages a mark only after a queued notice or an identical unread notice; any other policy refusal logs the outcome and leaves the row open for the next sweep (`src/CcDirector.Gateway/Messaging/FleetMessageStore.cs:403-447`). The system exemption still permits blank or overlong text to be refused (`src/CcDirector.Gateway/Messaging/FleetMessagePolicy.cs:133-138`), but the product-generated notice is nonblank and fits 16,000. A hand-made refused draft exercises the retry. Two messages to the same sender in one sweep have distinct full ids in their notice text at the product cap (`FleetDoorbell.cs:487-495`); thus they do not share a duplicate notice. Finding 2 states the cap boundary.
3. **Dictation warning: closed to the stated observed-run limit.** The per-tenant, per-session run begins on `DeferredDictation`, clears on any other attempt, and logs once strictly after 30 minutes (`src/CcDirector.Gateway/Messaging/FleetDoorbell.cs:331-361`). A ring between holds resets it; a later due deferral starts a new run. `NotDue` also clears it during ring grace (`FleetDoorbell.cs:368-371`). The warning is emitted only while a due ring is attempted, as the handoff states. A lock that ends and restarts entirely between observations is outside that observable run.
4. **Snooze: closed by the recorded owner decision.** This round changes no snooze code; the handoff records acceptance of the narrow exception and the settled-flicker attribution loss (`handoff.md:1057-1060,1125-1127`).
5. **Submit witness: accepted limit.** The code still infers submission from a new visible marker row or a working marker (`DoorbellSafety.cs:320-332`). A newly revealed old row or unrelated turn can supply that witness; the handoff now says so (`handoff.md:1128-1129`).

I found no additional contradiction to the fix-round brief or judgement calls 1 through 7. The newly disclosed inability to distinguish a trimmed character with an unchanged cursor is the exception to that conclusion.

## Whole-branch and test check

`git diff origin/main...HEAD --stat` covers 212 files. All touched paths are under the mission's brief, handoff, inspections, and proof evidence, or the message, doorbell, session, and snooze implementations and tests those handoff sections describe; I found no unexplained path. The added console timeline output is in the live proof harness (`src/CcDirector.Gateway.Tests/DoorbellEndToEndProof.cs`), and the best-effort cleanup comment is in a test fixture (`src/CcDirector.Gateway.Tests/SnoozeEndToEndTests.cs`); I found no leftover product debugging or commented-out implementation. The requested attribution-string sweep had hits in terminal capture fixtures and reader identifiers, and no commit or generation attribution line in the code diff.

Values that can change while these selected suites remain green include the 120 ms frame interval (`src/CcDirector.Core/Sessions/FleetDoorbellRinger.cs:107`), ten-second submit watch (`src/CcDirector.Core/Drivers/TerminalSubmit.cs:268`), ten erase polls (`FleetDoorbellRinger.cs:219`), eight-row bottom scan and four-row footer allowance (`DoorbellSafety.cs:123-126`), and six-hour stale-upload sweep with 24-hour pending age (`src/CcDirector.Gateway/GatewayHost.cs:852,880`). The new 30-minute warning threshold is pinned by a test (`src/CcDirector.Gateway.UnitTests/Messaging/FleetDoorbellTests.cs:786-790`); the fixed message cap is not pinned by the round's new tests.

Fresh runs on this tree: Core unit `DoorbellSafetyTests|FleetDoorbellRingerTests` **84 passed, 0 failed**; Gateway unit `FleetDoorbell*|FleetMessage*` **139 passed, 0 failed**. Both commands exited zero with nonempty test selections. These suites exercise their scripted frames and store cases, not the unchanged-cursor frame in finding 1 or a live terminal's handling of it.
