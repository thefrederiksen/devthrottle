[
 {"name":"item 8 reverted: every working edge ends an armed snooze","old":"            if (string.Equals(session.WorkingOrigin, WorkingOrigins.Agent, StringComparison.Ordinal))\n            {","new":"            if (false && string.Equals(session.WorkingOrigin, WorkingOrigins.Agent, StringComparison.Ordinal))\n            {"},
 {"name":"owner half weakened: any reported origin spares the snooze","old":"            if (string.Equals(session.WorkingOrigin, WorkingOrigins.Agent, StringComparison.Ordinal))\n            {","new":"            if (session.WorkingOrigin is not null)\n            {"},
 {"name":"the law weakened further: unexplained work spares the snooze too","old":"            if (string.Equals(session.WorkingOrigin, WorkingOrigins.Agent, StringComparison.Ordinal))\n            {","new":"            if (!string.Equals(session.WorkingOrigin, WorkingOrigins.Owner, StringComparison.Ordinal))\n            {"},
 {"name":"origin compared loosely","old":"            if (string.Equals(session.WorkingOrigin, WorkingOrigins.Agent, StringComparison.Ordinal))\n            {","new":"            if (string.Equals(session.WorkingOrigin?.Trim(), WorkingOrigins.Agent, StringComparison.OrdinalIgnoreCase))\n            {"},
 {"name":"agent-origin branch checked after the owner-turn edge is skipped","old":"        if (_registry.ClearIfSupersededByOwnerTurn(session.SessionId, session.LastOwnerTurnAtUtc))\n            return;","new":"        if (!string.Equals(session.WorkingOrigin, WorkingOrigins.Agent, StringComparison.Ordinal) && _registry.ClearIfSupersededByOwnerTurn(session.SessionId, session.LastOwnerTurnAtUtc))\n            return;"}
]
