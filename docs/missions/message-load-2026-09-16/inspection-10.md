# Inspection 10 — messaging core of the final branch

## Verdict: PASS for merge

Inspected detached `5fa11032`, the two specified source/tool diffs, their integration into the mission branch, and the merge with main. I found no merge blocker in this scope. The findings below are evidence limits and unpinned values, ranked by consequence; none reopens inspection 6 or 8. No product code was changed.

## Findings and limits, ranked

1. **Medium — a parked line's next ring is established by a scripted terminal frame, not a live terminal.** An unverified submit reads one frame and returns `deferred, parked` for anything except a readable empty composer (`src/CcDirector.Core/Sessions/FleetDoorbellRinger.cs:217-241`); it has no erase call. The next ring checks the composer before typing (`src/CcDirector.Core/Sessions/FleetDoorbellRinger.cs:149-152`; `src/CcDirector.Core/Drivers/DoorbellSafety.cs:175-196`). The exact and owner-extended frames are tested (`src/CcDirector.Core.UnitTests/Sessions/FleetDoorbellRingerTests.cs:220-251,301-363`), including inspection 8's unchanged-cursor case. Prove the remaining environment limit with a live unverified submit, then extend the parked line as the owner and trigger another due ring: expect `parked`, then `composer-holds-text`, no additional keystrokes, and no ring-count increment. The tests do not establish how every live terminal renders an invisible owner character. This does not revive deletion of owner input because the take-back and erase path is removed.

2. **Low — the selected suites leave some timing and screen-reading literals free to drift.** The 120 ms frame gap (`FleetDoorbellRinger.cs:107`), 10-second submit watch and 40 ms pre-Enter delay (`src/CcDirector.Core/Drivers/TerminalSubmit.cs:251,268`), eight-row bottom scan and four-row footer allowance (`DoorbellSafety.cs:123-126`), and the six-hour stale-upload sweep and 24-hour pending age (`src/CcDirector.Gateway/GatewayHost.cs:852-880`) can change without a literal assertion in these selections. Tests pin `parked` on both sides (`src/CcDirector.Core.UnitTests/Sessions/FleetDoorbellRingerTests.cs:444`; `src/CcDirector.Gateway.UnitTests/FleetDoorbellExecutorTests.cs:119`), the accepted reason list (`src/CcDirector.Gateway.Contracts/FleetMessageRequests.cs:282-315`), the text-cap floor 45 (`src/CcDirector.Gateway.UnitTests/Messaging/FleetDoorbellTests.cs:512-527`), and the default five-minute grace, three-ring cap, 15-second heartbeat, eight-way parallelism and five-second ring timeout (`FleetDoorbellTests.cs:621-623,1097-1099`). The selected tests do not pin every word of the stuck/no-reply notice or command-line status prose; changing unasserted wording could leave them green. Prove drift by mutating one literal in a disposable checkout and rerunning these filters; no mutation was made here.

## Reopening the earlier findings

- **Inspection 8, finding 1 — closed as ruled.** `FleetDoorbellRinger.UnverifiedAnswer` only reads a frame and returns `parked` unless it reads an empty composer (`src/CcDirector.Core/Sessions/FleetDoorbellRinger.cs:228-241`). `Session`'s composer erase method and the doorbell target's erase interface were removed by `3dacd248..8c2ebb3e`; `ComposerHoldsExactly` remains only as the submit echo witness (`FleetDoorbellRinger.cs:158-162`). A parked line blocks the next attempt as `composer-holds-text` in the scripted test (`FleetDoorbellRingerTests.cs:248-251`); extending it with a space, tab, non-breaking space, owner words, or an invisible unchanged-cursor character has the same no-erase result (`:233-244,301-363`). Gateway accepts `parked` as a known deferral and records no ring (`src/CcDirector.Gateway/Messaging/FleetDoorbell.cs:418-433`; `src/CcDirector.Gateway.Contracts/FleetMessageRequests.cs:306,315`). The screen witness can still miss a turn or a hidden character, but the product no longer deletes what it cannot distinguish.
- **Inspection 8, finding 2 — closed as ruled.** `MaxTextLength` rejects anything below `MinTextLength` on construction, including `with` copies (`src/CcDirector.Gateway/Messaging/FleetMessageLimits.cs:60-79`). The floor is the `Your message ` prefix plus the full 32-character id, 45 characters. `StuckNoticeText` rejects a lower explicit cap and truncates only after that full opening (`src/CcDirector.Gateway/Messaging/FleetDoorbell.cs:490-506`). Two different ids remain distinct at the floor (`src/CcDirector.Gateway.UnitTests/Messaging/FleetDoorbellTests.cs:510-533`).
- **Inspection 6, finding 1 — closed as ruled.** The merged `JudgeSystemNotice` judges text and duplicate policy, stages only a queued notice, and returns the verdict to both callers (`src/CcDirector.Gateway/Messaging/FleetMessageStore.cs:446-465`). The overdue path writes the mark with a queued notice or an identical unread notice about the same question; every other policy refusal leaves it unmarked for a later sweep (`:397-442`). The stuck path uses that same judge and refusal rule (`:670-720`). Product overdue text cuts only the roster name to fit the 16,000-character cap (`src/CcDirector.Gateway/Messaging/FleetMessageService.cs:246-287`). A too-long or blank test notice is refused and retried; an identical unread same-question notice permits the mark (`src/CcDirector.Gateway.UnitTests/Messaging/FleetMessageReplyStoreTests.cs:610-665`). For two overdue questions in one sweep, each draft carries its own `InReplyToMessageId` (`FleetMessageService.cs:249-255`), and the duplicate query includes that id (`FleetMessageStore.cs:257-264`); both get distinct notices. The selected tests exercise SQLite, not PostgreSQL or a simultaneous multi-process sweep.
- **Inspection 6, findings 2 and 3 — closed as ruled.** `ReadHistory` compares recipient, sender, unread text hash, kind, answered question id and the presence of a reply deadline (`src/CcDirector.Gateway/Messaging/FleetMessageStore.cs:248-289`). The two-question `yes` sequence queues a reply to each question; a plain unread `yes` does not swallow a reply (`src/CcDirector.Gateway.UnitTests/Messaging/FleetMessageReplyStoreTests.cs:306-344`). A reply-wanted send identical to an unread plain message queues a second row with a correlation id; the reverse plain-after-question case queues too (`:346-377`). A report identical to an unread ordinary message queues as its own kind (`:379-390`). The inbox renders distinct blocks with `kind`; only the reply-wanted block has `reply wanted by`, `correlation id`, and `to answer` lines (`tools/cc-devthrottle/src/session_ops.py:1830-1874`). No duplicate can silently turn a requested answer into the plain row under this key.
- **Inspection 6, colour-terminal finding — closed as ruled.** `_say_gateway` uses `highlight=False, soft_wrap=True`, preserving the Gateway sentence without inserted line breaks (`tools/cc-devthrottle/src/session_ops.py:1527-1542`). The fixture runs plain, colour and narrow colour terminals (`tools/cc-devthrottle/tests/conftest.py`), and the verbatim broadcast assertion is at `tools/cc-devthrottle/tests/test_message_queue.py:410-426`. The requested .NET selections do not execute that Python test, so this conclusion rests on the changed call and guard, not a fresh Python run.

## Routes that can reach a session

A session credential is checked against the method/path allow list before its identity is accepted (`src/CcDirector.Gateway/Util/AuthMiddleware.cs:513-528`). The list checks the exact `POST` shapes for prompt, interrupt, escape, fanout and verdict answer before general allowances (`src/CcDirector.Gateway/Util/SessionKeyGuard.cs:112-159`). The route census is:

| Route or trigger | What reaches a session | Session key? |
| --- | --- | --- |
| `POST /sessions/{sid}/prompt` (`src/CcDirector.Gateway/Api/GatewayEndpoints.cs:3436`) | Body text through Director `prompt` | **No**; guard `:154-159` |
| `POST /sessions/{sid}/interrupt`, `/escape` (`GatewayEndpoints.cs:3626-3650`) | Control keystrokes | **No**; same guard |
| `POST /fanout` (`GatewayEndpoints.cs:4660,4770-4805`) | One prompt per target | **No**; same guard |
| `POST /sessions/{sid}/turn-verdict/answer` (`GatewayEndpoints.cs:3090-3100`; `src/CcDirector.Gateway/Api/TunnelTurnVerdictAnswerChannel.cs:39`) | Chosen answer through `prompt` | **No**; same guard |
| `POST /sessions/{sid}/compact-context` with `continuePrompt` (`GatewayEndpoints.cs:4498-4518`) | Prompt after compaction | **No**; body-aware route check `:4504-4508`. Plain compaction is allowed. |
| `POST /sessions/{sid}/message`, `/fleet/broadcast`, `/fleet/reply` (`GatewayEndpoints.cs:3255-3300,3405-3413`; `SessionKeyGuard.cs:284-306,341-348`) | Queued inbox records, then a fixed doorbell line through `ring` on a due or settled edge (`src/CcDirector.Gateway/Messaging/FleetDoorbell.cs:225-284,371-433`; `src/CcDirector.Gateway/GatewayHost.cs:557-571`) | **Yes**, subject to message policy. The key cannot choose the doorbell's text or time, and `parked` records no ring. |
| `POST /dev-reports/{reportId}/send` (`src/CcDirector.Gateway/Api/DevReportEndpoints.cs:239-271`) | Owner items stored, delivered now if idle or at turn end if busy (`src/CcDirector.Gateway/DevReports/DevReportDelivery.cs:248-311`; `src/CcDirector.Gateway/GatewayHost.cs:3152-3155`) | **No**; absent from the guard's allow list (`SessionKeyGuard.cs:303-330,470-480`) and rejected again by the owner route (`DevReportEndpoints.cs:369-376`). A hosted route test sends a real session key and gets 403 with no item (`src/CcDirector.Gateway.Tests/DevReportRoutesHostedTests.cs:309-331`). |
| `POST /sessions/{sid}/wingman/voice-turn` (`src/CcDirector.Gateway/Api/GatewayWingmanVoiceEndpoint.cs:564-626`), `POST /dictation/{uploadId}/complete` (`src/CcDirector.Gateway/Api/GatewayDictationEndpoint.cs:275-282,737-750`) | Voice or dictation words through `prompt` | **No**; neither shape is on the session-key allow list (`SessionKeyGuard.cs:274-391`); dictation also checks a device credential. |
| Turn-end session rule firing (`src/CcDirector.Gateway/GatewayHost.cs:3146-3150`; `src/CcDirector.Gateway/Rules/GatewayRuleEnvironment.cs:161-172`) | Rule-generated prompt | **No direct session-key route**; the rule endpoints are absent from the allow list (`src/CcDirector.Gateway/Api/SessionRuleEndpoints.cs:48-83`; `SessionKeyGuard.cs:274-391`), and promotion requires an owner grant. |

The new-from-main dev-report session routes allow a session key to publish and read **its own** report and post a reply on it (`DevReportEndpoints.cs:64-68,136-156,346-360`; `SessionKeyGuard.cs:221-226,303-330`). They do not call delivery. The owner send is the only new route from main in this census that can cause a prompt. The delivery folds a random boundary around the owner's text and JSON-escapes page-written fields (`src/CcDirector.Gateway/DevReports/DevReportPromptFold.cs:9-23,51-88`). No route in this census lets a session key supply arbitrary text for an existing session's prompt.

## Integration and checks

The three integration merges and the merge with main left the shared notice judge at `FleetMessageStore.cs:446-465`, the accepted parked reason at `FleetDoorbell.cs:418-433`, and the inbox line on the display push. `DisplayFold` obtains the inbox source at fold time and supplies it only to a writing push (`src/CcDirector.Gateway/Fleet/DisplayFold.cs:49-70,73-116`); the host wires the store (`src/CcDirector.Gateway/GatewayHost.cs:1725`) and the row stamp takes `inboxLines` (`GatewayHost.cs:4907,4945`). A trace records colour and label without reading or stamping the inbox line, as slice 4 ruled. The route guard and dev-report routes added by main do not open a session-key prompt path.

Fresh runs on this detached head: Core unit `DoorbellSafetyTests|FleetDoorbellRingerTests`: **87 passed, 0 failed, 0 skipped**. Gateway unit `FleetDoorbell*|FleetMessage*|FleetReply*|SessionKeyGuard*`: **466 passed, 0 failed, 0 skipped**. Gateway route `FleetDoorbell*|FleetMessageRouteTests`: **46 passed, 0 failed, 0 skipped**. Both requested source/tool diffs passed `git diff --check`. The first Core attempt with `--no-restore` ran no tests because this worktree lacked assets; it was rerun with restore and the count above. These are SQLite, scripted terminal, and in-process route checks; they do not establish live terminal rendering, PostgreSQL behavior, or cross-process scheduling.
