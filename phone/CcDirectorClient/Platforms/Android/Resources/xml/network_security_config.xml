<?xml version="1.0" encoding="utf-8"?>
<!--
  Production reaches the Director/Gateway over Tailscale Serve (HTTPS), so cleartext
  stays forbidden by default: a plain-HTTP remote path must never exist (HTTPS-only).
  The hosts listed below are dev-only - the Android emulator's host-loopback alias
  (10.0.2.2) and localhost - used solely when testing against a Gateway/Director
  running on the same machine. They are never reachable in production, so allowing
  cleartext for just these hosts does not weaken the remote posture.
-->
<network-security-config>
  <base-config cleartextTrafficPermitted="false" />
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="false">10.0.2.2</domain>
    <domain includeSubdomains="false">localhost</domain>
    <domain includeSubdomains="false">127.0.0.1</domain>
  </domain-config>
</network-security-config>
