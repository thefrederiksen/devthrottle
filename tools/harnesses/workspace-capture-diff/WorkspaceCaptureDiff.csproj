<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>WorkspaceCaptureDiff</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- The FOLD only. This harness runs the same WorkspaceCapture the Gateway runs, over a roster read
         from a file, so the capture can be checked against a hand-written index without a Gateway. -->
    <ProjectReference Include="..\..\..\src\CcDirector.Gateway.Contracts\CcDirector.Gateway.Contracts.csproj" />
  </ItemGroup>

</Project>
