[project]
name = "cc-worktrees"
version = "0.1.0"
description = "Pooled git worktrees that are only reset when their work has provably landed"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
